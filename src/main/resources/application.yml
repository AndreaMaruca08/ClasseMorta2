spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ClasseMortaDB
    username: nuova_utente
    password: nuova_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false